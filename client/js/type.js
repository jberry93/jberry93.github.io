$(document).ready(function() {
  $(".type").typed({
    strings: ["developer", "builder", "foodie", "coffee lover", "pug fanatic"],
    typeSpeed: 0,
    loop: true,
    showCursor: true,
    cursorChar: "|"
  });
})
